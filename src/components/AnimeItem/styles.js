import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  info: {
    flex: 1,
    flexDirection: 'row',
  },
  image: {
    alignItems: 'stretch',
    width: 40,
    height: 60,
  },
});

export default styles;
